(*---------------------------------------------------------------------------
$(SRC_HEADER)
  ---------------------------------------------------------------------------*)

let () = ignore (Toploop.use_file Format.err_formatter "$(NAME,uncapitalize)_top_init.ml")

(*---------------------------------------------------------------------------
$(SRC_FOOTER)
  ---------------------------------------------------------------------------*)
